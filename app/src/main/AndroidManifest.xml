<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.voting_final_year_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Voting_final_year_project">
        <activity android:name=".forgetpassword"></activity>
        <activity android:name=".signup_activity" />
        <activity android:name=".signup" />
        <activity android:name=".inside_adminpanel" />
        <activity android:name=".votingpanel" />
        <activity android:name=".adminpanel" />
        <activity android:name=".mainscreen" />
        <activity android:name=".Walkthrogh" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>